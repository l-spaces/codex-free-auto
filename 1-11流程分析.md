# 1-11 流程分析（当前代码修订版）

## 0. 修订摘要（对比旧版 `1-11流程分析.md`）

本次按当前仓库代码重审后，已修正以下不一致点：

1. 删除了旧文档中不存在的引用（如 `shared/flow-registry.js`、`shared/settings-schema.js`、`stepExecutionRangeByFlow`、`assertNodeExecutionAllowedForState` 等）。
2. 修正步骤定义：当前不是“连续 1-11 共 11 个节点”，而是“编号区间 1-11 内仅有 10 个节点”，缺少第 9 步节点。
3. 修正执行器映射：当前 `stepExecutorsByKey` 不包含 `post-login-phone-verification` 节点。
4. 修正 sidepanel 语义：`select-flow` 在当前实现中用于选择 `executionMode`（`full-flow/register-gpt/authorize-codex`），不是切换 `openai/kiro` 这类业务 flow。
5. 重新梳理第 3 步：补齐 `NODE_COMPLETE -> finalizeStep3Completion -> PREPARE_SIGNUP_VERIFICATION` 的收尾链路。

## 1. 当前代码的流程定义入口

### 1.1 步骤定义来源

- 单一 flow：`openai`（`FLOW_DEFINITION_BUILDERS` 只注册了 `openai`）。
- 文件：`data/step-definitions.js:58-75`。

核心方法：

- `getSteps(options)`：返回当前步骤定义（含 `id/key/title`）。
- `getNodes(options)`：返回节点定义（含 `legacyStepId/nodeId/displayOrder`）。
- 文件：`data/step-definitions.js:124-147`。

### 1.2 background 如何拿步骤并执行

- `getStepDefinitionsForState`：从 `MultiPageStepDefinitions.getSteps` 取定义。
- `getNodeDefinitionsForState`：从 `MultiPageStepDefinitions.getNodes` 取节点。
- `getStepRegistryForState`：构建执行注册表。
- 文件：`background.js:391-409`、`background.js:457-477`、`background.js:11233-11242`。

执行核心：

- `executeNode`：执行单节点。
- `executeNodeAndWait`：执行并等待完成信号/后台收尾。
- `runAutoSequenceFromNodeGraph`：自动流程主循环。
- 文件：`background.js:9150`、`background.js:9261`、`background.js:10192`。

### 1.3 sidepanel 如何同步步骤

- `getStepDefinitionsForMode` / `getWorkflowNodesForMode` 从 `MultiPageStepDefinitions` 拉取步骤与节点。
- `syncStepDefinitionsForMode` -> `rebuildStepDefinitionState` 重建 UI 步骤列表。
- 文件：`sidepanel/sidepanel.js:377-395`、`sidepanel/sidepanel.js:397-425`、`sidepanel/sidepanel.js:3790-3809`。

## 2. 1-11 编号区间的实际节点映射

### 2.1 代码实测结果

通过 `data/step-definitions.js` 实测：

- 实际节点数量：`10`
- 节点 ID：`1,2,3,4,5,6,7,8,10,11`
- 缺失：`9`

对应定义来自：`createOpenAiAuthTail` 中直接使用了 `id+3`、`id+4`（跳过 `id+2`）。
- 文件：`data/step-definitions.js:16-24`。

### 2.2 步骤表（开发基准）

| 编号 | nodeId | 标题 | 主要执行器 |
| --- | --- | --- | --- |
| 1 | `open-chatgpt` | 打开 ChatGPT 官网 | `step1Executor.executeStep1` |
| 2 | `submit-signup-email` | 注册并输入邮箱 | `step2Executor.executeStep2` |
| 3 | `fill-password` | 填写密码并继续 | `step3Executor.executeStep3` |
| 4 | `fetch-signup-code` | 获取注册验证码 | `step4Executor.executeStep4` |
| 5 | `fill-profile` | 填写姓名和生日 | `step5Executor.executeStep5` |
| 6 | `wait-registration-success` | 等待注册成功 | `step6Executor.executeStep6` |
| 7 | `oauth-login` | 刷新 OAuth 并登录 | `step7Executor.executeStep7` |
| 8 | `fetch-login-code` | 获取登录验证码 | `step8Executor.executeStep8` |
| 9 | `（无）` | 当前无节点定义（历史空位） | `（无）` |
| 10 | `confirm-oauth` | 自动确认 OAuth | `step9Executor.executeStep9` |
| 11 | `platform-verify` | 平台回调验证 | `executeStep10` |

执行器映射位置：`background.js:11067-11077`。

## 3. 1-11 流程如何流转（按自动运行主链）

### 3.1 流程主骨架

1. 第 1 步：打开官网并清理 cookies。
- `background/steps/open-chatgpt.js:138-143`

2. 第 2 步：提交邮箱，确认落地页。
- 提交与重试：`background/steps/submit-signup-email.js:225-267`
- 完成上报（含 `skippedPasswordStep`）：`background/steps/submit-signup-email.js:278-285`

3. 第 3 步：填密码并继续（详见第 4 章）。
- `background/steps/fill-password.js:30-80`

4. 第 4 步：先做 `PREPARE_SIGNUP_VERIFICATION`，再执行验证码逻辑。
- `background/steps/fetch-signup-code.js:159-259`

5. 第 5 步：填资料。
- `background/steps/fill-profile.js:12-31`

6. 第 6 步：等待稳定，可写入账号、清 cookies。
- `background/steps/wait-registration-success.js:172-181`

7. 第 7 步：OAuth 登录，进入登录验证码或直接 OAuth 同意页。
- `background/steps/oauth-login.js:103-205`

8. 第 8 步：登录验证码轮询，失败时在“当前链路重试”与“回到步骤 7 重发链路”间切换。
- `background/steps/fetch-login-code.js:606-769`

9. 第 10 步：监听 localhost 回调（节点 `confirm-oauth`）。
- `background/steps/confirm-oauth.js:55-126`

10. 第 11 步：平台回调验证（CPA/SUB2API/Codex2API 分支）。
- 分发：`background/steps/platform-verify.js:230-238`

### 3.2 自动运行的编号边界

虽然当前实际节点缺少第 9 步，但自动运行边界仍按 `1-11` 管理：

- 默认范围：`startStep=1`、`endStep=11`
- `register_gpt`：`1-6`
- `authorize_codex`：`7-11`

代码位置：

- sidepanel：`sidepanel/sidepanel.js:326-329`、`sidepanel/sidepanel.js:1654-1660`
- background：`background.js:582-585`、`background.js:1822-1831`

## 4. 第 3 步如何跳转（重点）

## 4.1 第 3 步入口

- background 入口：`executeStep3(state)`
- 核心动作：生成/读取密码，向内容脚本发送 `EXECUTE_NODE(fill-password)`。
- 文件：`background/steps/fill-password.js:30-80`

## 4.2 内容脚本状态机（决定“跳转/跳过”）

第 3 步实际在 `step3_fillEmailPassword(payload)` 里执行：
- 文件：`content/signup-page.js:2540-2657`

状态探测函数：`inspectSignupEntryState()`：
- 文件：`content/signup-page.js:666-737`

关键分支：

1. 已在 `verification_page / phone_verification_page / profile_page / logged_in_home`
- 直接 `reportComplete(3, { skippedPasswordPage: true, ... })`
- 表示“第 3 步按跳过处理”。
- 代码：`content/signup-page.js:2561-2583`

2. 在 `email_entry`
- 先补做邮箱提交，再尝试进入密码页。
- 代码：`content/signup-page.js:2585-2592`

3. 在 `password_page`
- 先填密码。
- 先 `reportComplete(3, completionPayload)`。
- 再 `setTimeout` 异步点击提交（deferred submit）。
- 代码：`content/signup-page.js:2610-2654`

## 4.3 第 3 步完成后的后台收尾跳转

1. 收到 `NODE_COMPLETE(fill-password)` 后，message-router 特判收尾：
- `if (nodeId === 'fill-password') finalizeStep3Completion(...)`
- 文件：`background/message-router.js:677-703`

2. `finalizeStep3Completion` 注入到 `signupFlowHelpers.finalizeSignupPasswordSubmitInTab(...)`：
- 文件：`background.js:11111-11118`

3. `finalizeSignupPasswordSubmitInTab` 发送 `PREPARE_SIGNUP_VERIFICATION`：
- 文件：`background/signup-flow-helpers.js:244-289`

4. 内容脚本 `prepareSignupVerificationFlow` 决定真正落点：
- 进入验证码页：`ready=true`，正常进入步骤 4。
- 已进入 `step5` 或 `logged_in_home`：返回 `alreadyVerified=true`，可触发跳步。
- 仍在密码页：自动重填+重提交流程（最多 3 轮）。
- 文件：`content/signup-page.js:4598-4745`

## 4.4 第 3 步被第 2 步直接跳过

- 第 2 步完成时若落地页是验证码页，会在 payload 里带 `skippedPasswordStep`。
- 文件：`background/steps/submit-signup-email.js:278-285`

- message-router 处理 step2 payload 时，会把 step3 对应节点标记为 `skipped`。
- 文件：`background/message-router.js:573-579`

## 4.5 第 3 步异常跳转与失败

- 密码页若出现“手机号/密码不匹配”文案，内容脚本会抛 `SIGNUP_PHONE_PASSWORD_MISMATCH::...`。
- 文件：`content/signup-page.js:2738-2743`、`content/signup-page.js:4707-4709`

- 当前 background 未实现“该错误专用重启分支”，按通用失败流处理。
- `NODE_ERROR` 仅做失败状态与日志处理（含去重保护）。
- 文件：`background/message-router.js:761-776`

## 5. 第 10/11 步（OAuth 收尾）

1. 第 10 步 `confirm-oauth`
- 监听 localhost 回调。
- 捕获到地址后 `completeNodeFromBackground('confirm-oauth', { localhostUrl })`。
- 文件：`background/steps/confirm-oauth.js:55-126`

2. 第 11 步 `platform-verify`
- `executeStep10` 按 `panelMode` 分发：
  - `codex2api` -> `executeCodex2ApiStep10`
  - `sub2api` -> `executeSub2ApiStep10`
  - default -> `executeCpaStep10`
- 文件：`background/steps/platform-verify.js:230-238`

## 6. 关键方法与字段注释（开发排障必看）

### 6.1 关键方法

| 方法 | 中文注释（职责） |
| --- | --- |
| `executeNode(nodeId)` | 节点统一执行入口：设置状态、调用执行器、处理重试与失败收敛。 |
| `executeNodeAndWait(nodeId)` | 自动运行入口：根据节点类型决定“后台收尾”或“等待完成信号”。 |
| `completeNodeFromBackground(nodeId, payload)` | 后台直接收尾：置 `completed`、触发后置副作用、通知 waiter。 |
| `step3_fillEmailPassword(payload)` | 第 3 步核心页面状态机：识别页面状态、填密码、上报完成、异步提交。 |
| `prepareSignupVerificationFlow(payload)` | 第 3/4 步共享的“验证码前置准备器”：判定真实落点并自动恢复。 |

### 6.2 关键字段

| 字段 | 中文注释（含义） |
| --- | --- |
| `skippedPasswordStep` | 第 2 步落地时，是否已直接进入验证码页，供后台跳过第 3 步。 |
| `skippedPasswordPage` | 第 3 步执行时，页面已越过密码页（验证码/后续页）的标记。 |
| `signupVerificationRequestedAt` | 第 3 步触发提交时间戳，用于第 4 步验证码轮询窗口。 |
| `deferredSubmit` | 第 3 步是否采用“先上报完成，再异步点击提交”模式。 |
| `skipProfileStep` | 已进入登录态或资料已完成时，指示可跳过第 5 步。 |
| `prepareSource` / `prepareLogLabel` | `PREPARE_SIGNUP_VERIFICATION` 的来源标识与日志标签。 |
| `ready` / `alreadyVerified` | 前置准备结果：页面是否就绪，是否可按已验证状态直接推进。 |

## 7. 遗留兼容点（建议后续清理）

1. 步骤定义存在编号空洞：缺少 `id=9`。
- 来源：`data/step-definitions.js:22-23`（`id+3`、`id+4`）。

2. message-router 的默认映射仍按旧编号：`9 -> confirm-oauth`、`10 -> platform-verify`。
- 文件：`background/message-router.js:191-202`。

3. Step10 模块中的提示步号仍带旧逻辑（如 `resolveConfirmOauthStep` 返回 9）。
- 文件：`background/steps/platform-verify.js:54-56`。

4. background 常量中仍保留旧编号集合（如 `AUTO_RUN_BACKGROUND_COMPLETED_STEPS` 含 `9`、`STEP_COMPLETION_SIGNAL_STEPS` 含 `10/12`）。
- 文件：`background.js:8532-8547`。

这些兼容逻辑目前不一定会导致流程失败，但会增加“日志步号与真实节点”理解成本。
